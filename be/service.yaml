apiVersion: v1
kind: Service
metadata:
  name: flakeide-be
  namespace: flakeide-be
spec:
  type: NodePort
  selector:
    app: flakeide-be
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 33000
